import 'package:flutter/material.dart';

class OttDetailPage extends StatelessWidget {
  const OttDetailPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Column(
        children: [],
      ),
    );
  }
}
